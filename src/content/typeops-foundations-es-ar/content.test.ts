/**
 * Placeholder para tests de contenido (Hito 1)
 *
 * En el Hito 1 se agregarán:
 * - Schemas Zod del ContentPack
 * - Validación de IDs, referencias y prerrequisitos
 * - Walking skeleton de 1 item por modo
 * - Fixtures inválidos que deben fallar con mensaje específico
 *
 * Este archivo asegura que `npm run test:content` no falle
 * por ausencia de archivos en el Hito 0.
 */
import { describe, it, expect } from 'vitest'

describe('Corpus de contenido — Hito 0', () => {
  it('placeholder: el directorio de contenido existe y está preparado para Hito 1', () => {
    // Verificación trivial: el módulo de test carga correctamente
    expect(true).toBe(true)
  })
})
